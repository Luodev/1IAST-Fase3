# Tech Challenge — Fase 3
# Ambiente testado com Python 3.13 (miniconda) no Windows 11.
# Instalação:  pip install -r requirements.txt

# --- Dados -----------------------------------------------------------------
pandas>=2.0
numpy>=1.26
pyarrow>=14.0          # leitura/escrita Parquet da camada Gold local
requests>=2.31         # APIs públicas do IBGE

# --- Machine Learning ------------------------------------------------------
scikit-learn>=1.4      # pipelines, modelos, calibração, métricas
scipy>=1.11
statsmodels>=0.14      # suavização exponencial (Holt) na série nacional
shap>=0.44             # interpretabilidade por valores SHAP
joblib>=1.3            # persistência do artefato treinado

# --- Visualização ----------------------------------------------------------
matplotlib>=3.8
seaborn>=0.13

# --- Notebooks -------------------------------------------------------------
jupyter>=1.0
nbformat>=5.9
nbconvert>=7.10
ipython>=8.18
